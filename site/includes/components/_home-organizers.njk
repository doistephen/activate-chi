 {% import "components/_organizer.njk" as organizer %}
<section id="organizers">
    <div class="grid items-start justify-between grid-cols-12 gap-6 py-8">
    <div class="flex col-span-full">
        <h2 class="text-xs tracking-wider rounded-full text-hue font-display">Our organizers</h2>
    </div>
    <div class="max-w-xl col-span-8 space-y-10">
        <p class="text-2xl col-span-full">
            Our leadership team and members have the passion and dedication to take on the many challenges we face in the city of Chicago.
        </p>
    </div>
    <ul class="grid grid-cols-2 col-span-full gap-x-8 organizer-grid">
    {% for person in organizers %}
        {{ organizer.organizer(person.name, person.job, person.summary, person.image) }}
    {% endfor %}
    </ul>
</div>
</section>
