{% macro button(text, href, as="a", color=false, newTab=false, class, size="large") %}
    <{{as}}
    {% if href %}
        href="{{href}}"
    {% endif %}
    {% if newTab %}
        target="_blank"
    {% endif %}
        class="btn btn-outline
        {% if color %} border-hue border-opacity-60 btn-highlight{% endif %}
        {{class}}">
        <span>{{text}}</span>
    </{{as}}>
{% endmacro %}

{% macro iconButton(as="a", onClick, href, icon, class, size) %}
    <{{as}}
        {% if onClick %}
            @click="{{onClick}}"
        {% endif %}
        {% if href %}
            href="{{href}}"
        {% endif %}
        class="inline-flex flex-shrink-0 items-center justify-center transition border-white border-opacity-75 btn-outline
        {% if size === "small" %}
            w-9 h-9
        {% else %}
            p-4
        {% endif %}
        {{class}}">
        <span class="block text-current
            {% if size === "small" %}
                w-2.5 h-2.5
            {% else %}
                w-3.5 h-3.5
            {% endif %}"
        >{% include icon %}</span>
    </{{as}}>
{% endmacro %}

{% macro textButton(text, href, as="a", color=false, newTab=false, class) %}
    <{{as}}
    {% if href %}
        href="{{href}}"
    {% endif %}
    {% if newTab %}
        target="_blank"
    {% endif %}
        class="relative bg-link pb-1.5 pt-1 px-3 overflow-hidden border-link border-opacity-0 rounded-full text-sm block text-link bg-opacity-2 group-hover:bg-opacity-5 hover:bg-opacity-5 group-hover:border-opacity-25 group-focus:ring focus:ring transition
        {% if color %} text-hue {% endif %}
        {{class}}">
        <span>{{text}}</span>
        <div class="absolute inset-0 transition-colors bg-opacity-5 bg-link group-hover:bg-opacity-10"></div>
    </{{as}}>
{% endmacro %}
