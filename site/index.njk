---
layout: default
---
{% block body %}
<main class="space-y-8 md:space-y-16 lg:space-y-24">
    {# hero section #}
    {% include "components/_intro-section.njk" %}
    {# initiatives section #}
    {% include "components/_initiatives-section.njk" %}
    {# gallery section #}
    {% include "components/_gallery-section.njk" %}
    {# about section #}
    {% include "components/_about-section.njk" %}
    {# team section #}
    {% include "components/_organizers-section.njk" %}
    <section id="text" class="relative pt-24" x-data="{}">
        <h2 class="max-w-5xl mx-auto text-6xl leading-tight text-center text-hue font-display">
            <a id="text-cta" class="group"  href="sms:60611&body=ACTIVATE">Text <span class="text-white group-hover:underline">ACTIVATE</span> to <span class="text-white group-hover:underline">60611</span> to get action updates.</a>
        </h2>
        <div
        id="text-cta-hover"
        style="--tw-translate-x: var(--mouseX); --tw-translate-y: var(--mouseY);"
        class="fixed top-0 left-0 items-center justify-center invisible hidden w-32 h-32 p-4 text-base tracking-widest text-center uppercase transform border rounded-full opacity-0 pointer-events-none -rotate-3 font-display md:flex border-hue bg-gray">
            <span class="block pt-1">Yeah do it!</span>
        </div>
    </section>

    {# footer section #}
    {% include "components/_footer.njk" %}
</main>
{% endblock %}
